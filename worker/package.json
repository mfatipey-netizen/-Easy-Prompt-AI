{
  "name": "easy-prompt-ai-worker",
  "version": "1.0.0",
  "private": true,
  "description": "Secure backend API for EASY PROMPT AI (Cloudflare Worker).",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute easy-prompt-ai --file=./schema.sql",
    "db:init:local": "wrangler d1 execute easy-prompt-ai --local --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
